# Configuration template for the proposed MVP; application and Compose are not implemented yet.
APP_PORT=8000
POSTGRES_DB=eventmatch
POSTGRES_USER=eventmatch
POSTGRES_PASSWORD=replace_with_local_password
DB_HOST=db
DB_PORT=5432

# Keep the key empty for the planned deterministic fallback mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
AI_MODE=auto
AI_TIMEOUT_SECONDS=5
